# Mosquitto MQTT Broker Configuration
# Durian Garden IoT — Data Collection

# Listener (no auth for internal docker network — add TLS for production)
listener 1883
protocol mqtt

# Allow anonymous connections (docker-internal only)
allow_anonymous true

# Persistence
persistence true
persistence_location /mosquitto/data/

# Logging
log_dest stdout
log_type all
